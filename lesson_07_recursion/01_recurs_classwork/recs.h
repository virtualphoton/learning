#pragma once

// prints range from a to b including ends
void print_range(int a, int b);

// prints range from 1 to a including a
void print_range(int a);

// checks if string is palindrome
bool check_palindrome(const char * s, int start = 0, int fin = -1);
